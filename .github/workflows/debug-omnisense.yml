name: Debug Omnisense fetch

on:
  workflow_dispatch:
    inputs:
      site:
        description: 'Which Omnisense site to fetch'
        required: true
        default: 'tz'
        type: choice
        options: [tz, uk]

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install requests

      - name: Fetch Omnisense (debug mode)
        env:
          OMNISENSE_USERNAME: ${{ secrets.OMNISENSE_USERNAME }}
          OMNISENSE_PASSWORD: ${{ secrets.OMNISENSE_PASSWORD }}
        run: python fetch_omnisense.py --site "${{ inputs.site }}" --debug

      - name: Upload debug HTML responses
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: omnisense-debug-${{ inputs.site }}
          path: |
            dnld_rqst.html
            dnld_rqst5_response.html
          if-no-files-found: warn
